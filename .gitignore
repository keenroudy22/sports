__pycache__/
*.pyc
.env
.env.*
work/

# Page payloads are rebuilt from committed data on every deploy (scripts/build_site.py).
site/data/app/
# The plays feed and its cards are rebuilt and deployed the same way (scripts/feed.py).
site/data/feed.xml
site/data/cards/
.claude/
